<%= form_with(model: student_group, local: true) do |form| %>
  <% if student_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_group.errors.count, "error") %> prohibited this student_group from being saved:</h2>

      <ul>
      <% student_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :year %>
    <%= form.date_select :year_month, {:discard_day => true} %>
  </div>

  <div class="field">
    <%#= form.label :month %>
    <%#= form.date_select :month, id: :student_group_month %>
  </div>

  <div class="field">
    <%= form.label :course_name %>
    <%= form.text_field :course_name, id: :student_group_course_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
