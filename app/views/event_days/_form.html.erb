<%= form_with(model: event_day, local: true) do |form| %>
  <% if event_day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_day.errors.count, "error") %> prohibited this event_day from being saved:</h2>

      <ul>
      <% event_day.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :start_time %>
    <%= form.time_select :start_time, id: :event_day_start_time %>
  </div>

  <div class="field">
    <%= form.label :end_time %>
    <%= form.time_select :end_time, id: :event_day_end_time %>
  </div>

  <div class="field">
    <%= form.label :day %>
    <%= form.date_select :day, id: :event_day_day %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
